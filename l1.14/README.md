# L1.14 - Runtime type detection

Program determines the runtime type of a value passed as `interface{}`. Recognises `int`, `string`, `bool`, and any channel; everything else is marked as `unknown`.

## Run

```bash
cd l1.14
go run .
```

## Tests

```bash
cd l1.14
go test ./...
```
